{
  "name": "pulso-sync",
  "version": "1.0.0",
  "description": "SurveyMonkey → Firebase sync server for Control de Cuotas Pulso",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "firebase": "^10.12.0"
  }
}
